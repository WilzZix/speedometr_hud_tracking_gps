name: speedometr_hud_tracking_gps
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  onboarding: ^2.1.0
  introduction_screen: ^2.1.0
  speedometer: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
   assets:
      - assets/img/



